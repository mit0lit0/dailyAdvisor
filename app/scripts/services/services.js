'use strict';

/**
 * @ngdoc function
 * @name App.services
 * @description
 * # MainCtrl
 * Controller of the App
 */
angular.module('App')
  .factory('MainServices', function ($http, $q, dataService) {

    var factory = {};

    return factory;
  })
  .service('dataService', function(){

  });
